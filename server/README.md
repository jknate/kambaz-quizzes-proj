# Kambaz Quizzes Server

This is the Node.js backend server for the Kambaz Quizzes project.

## Getting Started

First, install the dependencies:

```bash
npm install
```

Then, start the server:

```bash
npm start
```

For development with auto-reload:

```bash
npm run dev
```

The server will start on port 4000 by default.